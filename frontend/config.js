const config = {
    // Update this with your Render backend URL after deployment
    API_BASE_URL: 'https://shoppie-backend.onrender.com',
    // Local development URL
    // API_BASE_URL: 'http://localhost:5000'
};

export default config; 